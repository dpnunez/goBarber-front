import styled, { css } from 'styled-components';

const Container = styled.div`
  position: absolute;
  right: 0;
  top: 0;
  padding: 30px;
  overflow: hidden;
  max-height: 100vh;
`;

export { Container };
